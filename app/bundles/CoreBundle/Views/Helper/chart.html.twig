<div class="chart-wrapper">
    <div class="pt-sd pr-md pb-md pl-md">
        <div style="height:{{ chartHeight }}px">
            <canvas class="chart {{ chartType }}-chart" {{ disabledLegned is defined ? 'data-disable-legend' : '' }}>
                {{ chartData|json_encode }}
            </canvas>
        </div>
    </div>
</div>
